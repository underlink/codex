services:
  - type: worker
    name: telegram-video-downloader-bot
    runtime: docker
    plan: starter
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: MAX_VIDEO_MB
        value: "45"
      - key: DOWNLOAD_TIMEOUT_SECONDS
        value: "180"
