services:
  - type: web
    name: telegram-video-downloader-bot
    runtime: docker
    plan: free
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: MAX_VIDEO_MB
        value: "45"
      - key: DOWNLOAD_TIMEOUT_SECONDS
        value: "180"
